<h1> Вопрос по <%= @question.test.title %></h1> 

<p>
  <%= @question.body %>
</p>  


<!-- Пока не использую паршал -->
<% render 'question' %>

<%= link_to "Back", root_path %>
<%= link_to "Delete", question_path(@question), method: :delete, data: {confirm: "Удаляем?"} %>

