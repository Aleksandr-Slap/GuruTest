<h1> Вопрос по <%= @question.test.title %></h1> 

<p>
  <h2><%= @question.body %></h2>
</p> 

<%= render @question.answers %>

<%= link_to "Back", test_path(@question.test) %> |
<%= link_to "Create new answer",new_question_answer_path(@question) %> |
<%= link_to "Delete", question_path(@question), method: :delete, data: {confirm: "Удаляем вопрос?"} %>

